<Window 
    x:Class="BDC_V1.Views.LoginView"        
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:utils="clr-namespace:BDC_V1.Utils"
    prism:ViewModelLocator.AutoWireViewModel="True"
    WindowStyle="None"
    Background="{DynamicResource MaterialDesignPaper}"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    utils:DialogCloserExtension.DialogResult="{Binding Path=DialogResultEx}"
    mc:Ignorable="d" 
    Height="450" Width="800"
    d:DesignHeight="450" d:DesignWidth="800">

    <Window.Resources>
        <ResourceDictionary>
            <!--Since this is specified in multiple locations it should go into a
            common location instead.-->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </Window.Resources>

    <Grid>
        <StackPanel
            HorizontalAlignment="Center" 
            VerticalAlignment="Center">

            <Label 
                Margin="5" 
                Content="{Binding LabelContent}" />

            <Button
                Margin="5"
                Style="{StaticResource MaterialDesignRaisedAccentButton}"
                Width="150"
                Command="{Binding CmdLogin}"
                Content="{Binding LoginButtonContent}"/>
        </StackPanel>


    </Grid>
</Window>
