<Window 
    x:Class="BDC_V1.Views.LoginView"        
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:local="clr-namespace:BDC_V1"
    xmlns:utils="clr-namespace:BDC_V1.Utils"
    xmlns:vm="clr-namespace:BDC_V1.ViewModels"
    xmlns:validationRules="clr-namespace:BDC_V1.ValidationRules"
    prism:ViewModelLocator.AutoWireViewModel="True"
    WindowStyle="None"
    Background="{DynamicResource MaterialDesignPaper}"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    mc:Ignorable="d" 
    Height="450" Width="800"
    >

    <Window.Resources>
        <ResourceDictionary>
            <!-- note you only need bring in these extra resource dictionaries when using non-default styles, so only bring them into your controls where the default style is not what you want -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ComboBox.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Slider.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBox.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Dark.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Border Background="{DynamicResource PrimaryHueMidBrush}"/>

        <StackPanel
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Center"
            Margin="16"
        >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" 
                            VerticalAlignment="Top">
                    <TextBlock
                        Style="{StaticResource MaterialDesignTitleTextBlock}"
                        HorizontalAlignment="Center"
                        Margin="2">
                        Builder Data Collector ~ Wireframe 1.0
                    </TextBlock>
                    <TextBlock
                        Style="{StaticResource MaterialDesignSubheadingTextBlock}"
                        HorizontalAlignment="Center"
                        Margin="2">
                        Version 1.0
                    </TextBlock>
                    <Image Width="200" 
                           Source="../Resources/CardnoLogo.bmp"
                           Margin="0 2 0 16"/>
                    <!--                    <DockPanel Height="100" LastChildFill="False"/>-->
                </StackPanel>

                <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4" Margin="0 0 0 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                        Style="{StaticResource MaterialDesignSubheadingTextBlock}">
                        BUILDER project configuration file:
                    </TextBlock>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Grid.Column="0" Kind="File" Foreground="{Binding ElementName=NameTextBox, Path=BorderBrush}" />
                        <TextBox Grid.Column="1"
                                 x:Name="ConfigFileTextBox"
                                 materialDesign:HintAssist.Hint="&lt; Select configuration file for this BUILDER FCA project &gt;"
                                 BorderThickness="1"
                                 BorderBrush="Black"
                                 VerticalContentAlignment="Center">
                        </TextBox>
                        <materialDesign:Badged Grid.Column="2"
                                               x:Name="SelectConfigFile"
                                               BadgeColorZoneMode="PrimaryDark">
                            <Button Margin="10 0 0 0"
                                Style="{StaticResource MaterialDesignRaisedLightButton}"
                                Click="{Binding SelectConfigFileButtonOnClick}">
                                Select...
                            </Button>
                        </materialDesign:Badged>
                    </Grid>
                </Grid>

                <Grid Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="4" Margin="0 0 0 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                        Style="{StaticResource MaterialDesignSubheadingTextBlock}">
                        BRED file to use:
                    </TextBlock>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Grid.Column="0" Kind="FileAccount" Foreground="{Binding ElementName=NameTextBox, Path=BorderBrush}" />
                        <TextBox Grid.Column="1"
                            x:Name="SelectQcFileTextBox"
                            materialDesign:HintAssist.Hint="&lt; Select your BRED file that you downloaded from BUILDER QC &gt;"
                            BorderThickness="1"
                            BorderBrush="Black"
                            VerticalContentAlignment="Center">
                        </TextBox>
                        <materialDesign:Badged Grid.Column="2"
                            x:Name="SelectQcFile"
                            BadgeColorZoneMode="PrimaryDark">
                            <Button Margin="10 0 0 0"
                                Style="{StaticResource MaterialDesignRaisedLightButton}"
                                Click="{Binding SelectQcFileButtonOnClick}">
                                Select...
                            </Button>
                        </materialDesign:Badged>
                    </Grid>
                </Grid>

                <Grid Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="3" Margin="0 0 0 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                        Style="{StaticResource MaterialDesignSubheadingTextBlock}">
                        Inspector:
                    </TextBlock>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Grid.Column="0" Kind="AccountArrowRightOutline" Foreground="{Binding ElementName=NameTextBox, Path=BorderBrush}" />
                        <ComboBox Grid.Column="1" 
                            materialDesign:HintAssist.Hint="&lt; Select your name from the pull-down list (or click More) &gt;"
                            MinWidth="72"
                            Margin="0 0 0 0"
                            VerticalAlignment="Bottom"
                            BorderThickness="1"
                            BorderBrush="Black"
                            ItemsSource="{Binding LoginUserList}"
                            SelectedValue="{Binding SelectedLoginUser}"
                            >
                            <ComboBox.SelectedItem>
                                <Binding
                                    Path="SelectedLoginUser"
                                    Mode="TwoWay"
                                    UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <validationRules:NotEmptyValidationRule
                                            ValidatesOnTargetUpdated="True" />
                                    </Binding.ValidationRules>
                                </Binding>
                            </ComboBox.SelectedItem> -->
                            <ComboBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel />
                                </ItemsPanelTemplate>
                            </ComboBox.ItemsPanel>
                        </ComboBox>
                        <materialDesign:Badged Grid.Column="2"
                            x:Name="SelectInspectorMore"
                            BadgeColorZoneMode="PrimaryDark">
                            <Button Margin="10 0 0 0"
                                Style="{StaticResource MaterialDesignRaisedLightButton}"
                                Click="{Binding SelectInspectorButtonOnClick}">
                                More...
                            </Button>
                        </materialDesign:Badged>
                    </Grid>
                </Grid>

                <materialDesign:Badged Grid.Row="8" Grid.Column="1" Margin="0 0 0 0"
                    x:Name="Login"
                    BadgeColorZoneMode="PrimaryDark"
                    HorizontalAlignment="Center"
                    >
                    <Button Margin="10 0 0 0"
                        Style="{StaticResource MaterialDesignRaisedLightButton}"
                        Click="{Binding LoginButtonOnClick}"
                        HorizontalAlignment="Stretch"
                        BorderThickness="1"
                        BorderBrush="Black"
                        VerticalContentAlignment="Center">
                        <Button.Template>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Grid>
                                    <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                </Grid>
                            </ControlTemplate>
                        </Button.Template>
                        LOG IN
                    </Button>
                </materialDesign:Badged>

            </Grid>

        </StackPanel>

    </Grid>
</Window>
